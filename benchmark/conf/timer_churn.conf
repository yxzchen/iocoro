# timer_churn
# fields: sessions (concurrency), waits (timer waits per session)
ITERATIONS=5
WARMUP=1
TIMEOUT_SEC=120
SCENARIO_ROWS=(
  "sessions=1 waits=200000"
  "sessions=8 waits=80000"
  "sessions=32 waits=20000"
)
