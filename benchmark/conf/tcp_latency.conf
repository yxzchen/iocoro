# tcp_latency
# fields: sessions (concurrency), msgs (messages per session), msg_bytes (payload size)
ITERATIONS=5
WARMUP=1
TIMEOUT_SEC=90
SCENARIO_ROWS=(
  "sessions=1 msgs=5000 msg_bytes=64"
  "sessions=8 msgs=1000 msg_bytes=64"
  "sessions=8 msgs=500 msg_bytes=1024"
)
